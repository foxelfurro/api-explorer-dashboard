# URL pública del backend API (usada por el frontend)
VITE_API_BASE_URL=http://localhost:3000

# URL de base de datos (referencia para el backend/infra)
# Nota: este valor NO es consumido directamente por el frontend.
DATABASE_URL=postgresql://user:password@localhost:5432/inventory_db
